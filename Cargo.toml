[package]
name = "voicemeeter-aec"
version = "1.2.1"
edition = "2024"
license = "GPL-3.0-only"

[dependencies]
sonora = "=0.2.0"

[build-dependencies]
cc = "1"

[patch.crates-io]
sonora = { path = "patches/sonora" }

[profile.release]
lto = "thin"
